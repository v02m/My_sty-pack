% ===========================
% 📦 ЯЗЫКИ И ЛОКАЛИЗАЦИЯ
% ===========================
\ProvidesPackage{langsetup}[2025/04/21 Language and locale setup]
\RequirePackage{polyglossia}  % 📦 Пакет polyglossia обеспечивает поддержку многоязычности в XeLaTeX и LuaLaTeX.

% 🌍 Установка основного языка
\setmainlanguage[
spelling=modern,      % ⚙️ Опция 'spelling=modern' устанавливает современное правописание для русского языка.
babelshorthands=true  % ⚙️ Опция 'babelshorthands=true' включает поддержку типографических сокращений, принятых в русской типографике (например, << >>, --, ---).
]{russian}

% 🇬🇧 Установка дополнительного языка (если необходим)
\setotherlanguage{english}  % ⚙️ Команда \setotherlanguage позволяет определить дополнительный язык для использования в документе.

% ⚙️ Дополнительные настройки для основного языка (если потребуются)
% 🇷🇺 В данном случае мы явно устанавливаем опцию babelshorthands=true для русского языка.
%    Это может быть полезно, если в будущем вы захотите переопределить какие-либо другие настройки языка.
\setkeys{russian}{babelshorthands=true}

% 📌 Примечания:
% 🔗 `polyglossia` тесно взаимодействует с `fontspec` (который мы подключаем в `fontswitch.sty`)
%    и активируется только при использовании XeLaTeX или LuaLaTeX.
% 🖋️ Опция 'babelshorthands=true' включает поддержку типографических сокращений в русском языке,
%    что соответствует рекомендациям ГОСТ для оформления текстов.

\endinput % 🔚 Конец файла пакета.