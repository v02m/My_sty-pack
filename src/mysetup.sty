% ===========================
% ⬛ БАЗОВАЯ ИНФОРМАЦИЯ О ПАКЕТЕ
% ===========================

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{mysetup}[2025/04/21 Custom LaTeX setup by Valery]

% 📌 Обязательные зависимости
\RequirePackage{iftex}
\RequirePackage{etoolbox}

% ===========================
% 🧠 ОПРЕДЕЛЕНИЕ КЛАССОВ ДОКУМЕНТА
% ===========================
% Флаги классов — используются во всех модулях
%% 📌 Определение класса (эти флаги должны быть определены в mysetup.sty)
%Эта секция кода автоматически определяет, какой класс документа
%используется (memoir, article или beamer) и устанавливает
%соответствующий логический флаг в значение "истина".
%Это позволяет в дальнейшем в коде документа или в других стилях
%условно выполнять определенные действия или включать/исключать контент в зависимости от типа документа.
%Например, можно определить специфическое оформление для книг (класс memoir) или для презентаций (класс beamer).
\newif{\ifmemoir}
\newif{\ifarticle}
\newif{\ifbeamer}

\@ifclassloaded{memoir}{\memoirtrue}{}
\@ifclassloaded{article}{\articletrue}{}
\@ifclassloaded{beamer}{\beamertrue}{}


% ===========================
% 🧩 УСЛОВНЫЕ ФЛАГИ РЕЖИМОВ (можно задавать в документе)
% ===========================
%Эта секция определяет дополнительные логические флаги, которые не зависят от класса документа,
%а скорее представляют собой различные "режимы" оформления.
%Автор документа может устанавливать значения
%\strictlayouttrue/\strictlayoutfalse и \booklayouttrue/\booklayoutfalse в своем основном .tex-файле,
%чтобы активировать или деактивировать соответствующее оформление, определенное в стилях.
\newif{\ifstrictlayout}   % для ГОСТ-документов
\newif{\ifbooklayout}     % для макета книги

% ===========================
% ⚙️ НАСТРОЙКА ШРИФТОВ
% ===========================
\newif{\ifusemsfonts}        % Флаг для использования шрифтов Microsoft
\newif{\ifuseliberationfonts} % Флаг для использования шрифтов Liberation
\newif{\ifusecmufonts}      % Флаг для использования шрифтов Computer Modern Unicode

\usemsfontstrue % <--- ВЫБЕРИТЕ ОДИН И УСТАНОВИТЕ В TRUE


% ===========================
% 📦 МОДУЛИ ПОДКЛЮЧАЮТСЯ ЗДЕСЬ
% ===========================

% Шрифты, язык, кодировка
\RequirePackage{fontswitch}       % Подключение системных шрифтов
\RequirePackage{langsetup}        % polyglossia + язык

% Геометрия и структура
%\RequirePackage{geometrysetup}    % Параметры страницы
\RequirePackage{layoutsetup}      % chapterstyle, pagestyle, темы

% Библиография
\RequirePackage{bibsetup}         % biblatex + gost + csquotes

% Математика и окружения
\RequirePackage{mathsetup}

% Таблицы, графика, списки
\RequirePackage{tablesetup}
\RequirePackage{graphicssetup}
\RequirePackage{enumsetup}
\RequirePackage{listingssetup}

% Заметки и отладка
\RequirePackage{todo-setup}

% PDF и типографика
\RequirePackage{pdf-setup}


% ===========================
% 🔌 ДОПОЛНИТЕЛЬНЫЕ МОДУЛИ (условно)
% ===========================
\ifbeamer
\RequirePackage{mysetup-beamer} % Подключаем специфичные настройки для beamer
\fi

\ifmemoir
\RequirePackage{mysetup-memoir} % Подключаем специфичные настройки для memoir
\fi

% ===========================
% 🔧 ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
% ===========================

% Поддержка кавычек для цитат
\RequirePackage{csquotes}

% Дополнительная типографика
\RequirePackage{microtype}

% ===========================
% 🔌 ДОПОЛНИТЕЛЬНЫЕ ПАКЕТЫ (если нужны)
% ===========================
% Можно подключать здесь специфичные вещи, напр. для журнала или конференции
% \RequirePackage{custom-journal}

\endinput