% ===========================
% 🔢 ГОСТ-ОФОРМЛЕНИЕ СПИСКОВ
% ===========================

\ProvidesPackage{enumsetup}[2025/04/21 Lists and enumeration (GOST)]

\RequirePackage{enumitem}

% --- Поддержка русских букв в нумерации ---
\makeatletter
\AddEnumerateCounter{\asbuk}{\russian@alph}{щ} % Добавляем asbuk как счётчик
\makeatother

% --- Определение визуальных маркеров для маркированных списков ---
\newcommand{\cdash}{\textemdash} % длинное тире для списков (ГОСТ)

\renewcommand{\labelitemi}{\normalfont\rmfamily{\cdash}} % 1-й уровень: длинное тире
\renewcommand{\labelitemii}{\normalfont\rmfamily{--}}    % 2-й уровень: обычное тире
\renewcommand{\labelitemiii}{\normalfont\rmfamily{*}}    % 3-й уровень: звёздочка

% --- Нумерация по ГОСТ ---
\renewcommand{\theenumi}{\asbuk{enumi}}  % 1-й уровень: а), б), в)...
\renewcommand{\labelenumi}{\theenumi)}

\renewcommand{\theenumii}{\arabic{enumii}} % 2-й уровень: 1), 2), 3)...
\renewcommand{\labelenumii}{\theenumii)}

\renewcommand{\theenumiii}{\arabic{enumiii}} % 3-й уровень
\renewcommand{\labelenumiii}{\theenumiii)}

% --- Глобальные параметры оформления списков ---
\setlist{
	nosep,                          % Без лишних отступов
	labelindent=\parindent,        % Абзацный отступ перед маркером
	leftmargin=*                   % Автоматическое выравнивание по метке
}

% 📎 Примечание:
% ГОСТ 2.105-95 требует оформлять списки с длинным тире, либо с нумерацией по буквам.
% Пакет `enumitem` позволяет гибко настраивать маркеры и отступы.
