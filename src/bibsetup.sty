% ===========================
% 📚 БИБЛИОГРАФИЯ
% ===========================

\ProvidesPackage{bibsetup}[2025/04/21 Bibliography and citations]

\RequirePackage{csquotes}  % Поддержка кавычек, зависит от языка
\RequirePackage[
backend=biber,         % Используем biber как движок
style=gost-numeric,    % ГОСТ с числовыми ссылками (есть и другие: gost-author-year и т.д.)
language=auto,         % Автоматический выбор языка (на основе polyglossia)
bibencoding=utf8,      % Кодировка библиографии
sorting=nyt,           % Сортировка: автор → год → заглавие
autocite=inline,       % Вставка цитат по \autocite
doi=true,              % DOI в списке
url=true,              % URL-адреса
isbn=false             % ISBN не отображаем
]{biblatex}

% Путь к файлу библиографии (можно изменить по месту)
\addbibresource{bibliography.bib}

% Примечания:
% - Стиль `gost-numeric` входит в `biblatex-gost`, который нужно установить отдельно.
% - Вы можете использовать `\cite`, `\autocite`, `\textcite` и т.д.
% - Если нужны авторо-годовые ссылки — замените `style=gost-numeric` на `gost-author-year`.

% 📌 Пример вставки:
%     \autocite{Петров2020}
%     \printbibliography
