% ===========================
% 📦 ЯЗЫКИ И ЛОКАЛИЗАЦИЯ
% ===========================
\ProvidesPackage{langsetup}[2025/04/21 Language and locale setup]
\RequirePackage{fontspec} % Современный пакет работы со шрифтами
\RequirePackage{polyglossia}  % Поддержка многоязычности в XeLaTeX и LuaLaTeX

% Установка языков
\setmainlanguage[
spelling=modern,         % Современное правописание
babelshorthands=true     % Поддержка сокращений типа << >>, -- и т.п.
]{russian}

\setotherlanguage{english}   % Дополнительный язык: английский

% Примечания:
% 📌 `polyglossia` работает в паре с `fontspec`,
% и подгружается только в XeLaTeX или LuaLaTeX.
% 📌 babelshorthands=true включает поддержку типографических
% сокращений в русском языке (ГОСТ допускает).

% Вынос всего, что относится к языковой логике, в отдельный файл.
%Гарантируем совместимость с fontswitch.sty, который уже отвечает за fontspec.
% Установка основного и дополнительного языков

\setkeys{russian}{babelshorthands=true}

% Настройка основного шрифта
\setmainfont[ Ligatures=TeX, BoldFont=Times New Roman Bold, ItalicFont=Times New Roman Italic ]{Times New Roman}

% Шрифт без засечек
\setsansfont{Arial}

% Моноширинный шрифт
\setmonofont[Scale=MatchLowercase]{Courier New}
