% ===========================
% 📐 Настройка модуля таблиц
% ===========================
% mysetup-tables.sty — модуль таблиц
\ProvidesPackage{tablesetup}[2025/04/24 Modular math support by Valery]

% Базовые пакеты
\RequirePackage{longtable}   % Таблицы, которые могут занимать несколько страниц
\RequirePackage{array}       % Расширенные возможности для определения столбцов
\RequirePackage{tabularx}    % Таблицы с автоматической шириной столбцов
\RequirePackage{dcolumn}     % Выравнивание чисел по десятичной точке
\RequirePackage{booktabs}    % Улучшенные горизонтальные линии в таблицах
\RequirePackage{caption}     % Настройка подписей к таблицам

% Расширенные таблицы
\RequirePackage{multirow}    % Ячейки, занимающие несколько строк
\RequirePackage{makecell}    % Дополнительное форматирование содержимого ячеек
\RequirePackage{rotating}    % Поворот текста и таблиц


% Таблицы на несколько страниц + автоширина (xltabular уже включает longtable и tabularx)
\RequirePackage{ltcaption}   % Подписи к длинным таблицам
\RequirePackage{xltabular}   % Заменяет longtable + tabularx


% Пользовательские команды
%Надо заменить
\newcommand{\Нстрочка}{\tabularnewline \hline}
%🔹 Упрощение для русифицированной разметки таблиц: эквивалент \\ \hline
%✳️ Можно переименовать в \newrow, чтобы избежать кодировки/совместимости,
%но в рамках русского стиля — допустимо.

% Здесь можно добавить дополнительные настройки для таблиц, если потребуется.
\endinput