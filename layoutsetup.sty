% ===========================
% 🧱 ОФОРМЛЕНИЕ РАЗДЕЛОВ И СТРАНИЦ
% ===========================
\ProvidesPackage{layoutsetup}[2025/04/21 Layout and header/footer setup]

\RequirePackage{etoolbox}   %%%%Для \ifclassloaded

% ===========================
% A R T I C L E
% ===========================
\ifarticle
	\RequirePackage{fancyhdr}
	\pagestyle{fancy}
	\fancyhf{}                 % Очистить колонтитулы
	\rhead{\thepage}           % Только номер страницы в правом верхнем углу
\fi

% ===========================
% B E A M E R
% ===========================
\ifbeamer
	\RequirePackage{tikz}
% Темы Beamer следует настраивать в mysetup-beamer.sty
% \usetheme{Madrid}          % Пример базовой темы (перенесено в mysetup-beamer.sty)
	\setbeamertemplate{navigation symbols}{} % Удаление кнопок
\fi

% 📎 Примечание:
% - В `article` применяется `fancyhdr`.
% - Для оформления `memoir` используются настройки в `mysetup-memoir.sty`.
% - Для презентаций `beamer` основные темы оформляются в `mysetup-beamer.sty`.

%%%% ===========================
%%%% M E M O I R
%%%% ===========================
%%%\ifmemoir
%%%% Стиль глав и стиль страниц
%%%\chapterstyle{dash}        % 📘 Оформление заголовков глав
%%%\pagestyle{headings}       % Колонтитулы с названиями секций
%%%\fi

%Основные изменения:
%
%Удалены настройки memoir: Специфичные для memoir строки (\chapterstyle{dash} и \pagestyle{headings}) были удалены из этого файла.
%Эти настройки теперь следует разместить в файле mysetup-memoir.sty.
%Перенесена тема Beamer: Команда \usetheme{Madrid} была закомментирована и перенесена в mysetup-beamer.sty,
%поскольку темы Beamer являются специфичными для этого класса.
%Сохранены настройки для article и beamer: Настройки для fancyhdr в article и удаление символов навигации в beamer остались,
%так как они относятся к этим классам.
%Обновлены комментарии: Комментарии были обновлены,
%чтобы отразить, где теперь находятся специфичные настройки для memoir и Beamer.

