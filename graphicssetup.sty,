% ===========================
% 🖼️ РАБОТА С ИЗОБРАЖЕНИЯМИ И ГРАФИКОЙ
% ===========================

\ProvidesPackage{graphicssetup}[2025/04/21 Graphics and illustrations]

% Основной пакет для вставки изображений
\RequirePackage{graphicx}
\graphicspath{{img/}}  % Папка для изображений

% Работа с SVG (опционально, если установлен inkscape)
\RequirePackage{svg}

% Вставка внешних PDF-страниц
\RequirePackage{pdfpages}

% Альбомные страницы
\RequirePackage{pdflscape}

% Поддержка подписей к изображениям и таблицам
\RequirePackage[font={normal}]{caption}
\RequirePackage{subcaption}  % Подписи к подрисункам

% Вращение объектов (например, таблиц)
\RequirePackage{rotating}

% TikZ для векторной графики
\RequirePackage{tikz}
\usetikzlibrary{chains, shapes.geometric, shapes.symbols, arrows}

% 📌 Примечание:
% TikZ-библиотеки позволяют строить диаграммы, схемы, графы, и пр.
% Подписи (caption/subcaption) оформляются в соответствии с ГОСТ
% svg требует отдельной установки Inkscape и компиляции через `--shell-escape`.
